pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230911073212/test_queue_freeze_checks_end_emitted_anyway/main
  - label=queue
  name: queue_to_default
queue_rules:
- merge_conditions:
  - check-success=pending_check
  name: default
